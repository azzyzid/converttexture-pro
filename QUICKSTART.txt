CONVERTTEXTURE PRO v2.2 — NETLIFY FIX

1. Upload/replace seluruh source ke repository GitHub.
2. Pastikan ada:
   netlify/functions/api.mjs
   netlify.toml
   package.json
   public/_redirects
3. Netlify publish directory harus: public
4. Trigger deploy → Clear cache and deploy site.
5. Buka /api/health. Harus menampilkan JSON "ok": true.
6. Login memakai data admin yang dikirim secara privat di chat.
7. Setelah berhasil login, segera ganti password admin.

Registrasi, login, queue, quota, dan log disimpan server-side memakai Netlify Blobs.
Tidak perlu MySQL atau database eksternal.
