{
  "name": "converttexture-pro",
  "version": "2.2.0",
  "private": true,
  "description": "Minecraft resource-pack converter and optimizer with Netlify Functions authentication, quota, queue, and admin logs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "netlify dev",
    "check": "node --check server.js && node --check public/app.js && node --check public/tools.js && node --check netlify/functions/api.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@netlify/blobs": "10.7.9"
  }
}
